The simple app is built with C#, MVC 5, bootstrap, Entity Framework and Visual Studio 2019

To run the application, simply set the connection string to an SQL Server instance in web.config and run the application

It is an EF Code-First so the database will be automatically created.